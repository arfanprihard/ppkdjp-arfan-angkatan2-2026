import { publicClient } from './client.js';

// TODO: Buat fungsi loginAPI(username, password) untuk mengirim POST request ke '/login'
// Kirim data username dan password di body request.
// Kembalikan data responnya: return response.data;
export const loginAPI = async (username, password) => {
  // Tulis kode Anda di sini...
};
